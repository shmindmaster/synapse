########################
# Dependencies (Node)
########################
node_modules/
.pnpm-store/
.npm/
.pnp*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

########################
# Build / Output / Cache (Next/Vite/TS)
########################
.next/
out/
dist/
build/
.output/
.astro/
.turbo/
.cache/
.vite/
.vite*/
.parcel-cache/
.nyc_output/
.eslintcache
.stylelintcache
*.tsbuildinfo
next-env.d.ts

########################
# Test artifacts
########################
coverage/
.coverage
.coverage.*
coverage.xml
test-results/
playwright-report/
blob-report/

########################
# Python (runtime + tooling caches)
########################
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
__pypackages__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.nox/
.ipynb_checkpoints/

########################
# Env / secrets (keep examples)
########################
.env
.env.*
.env.local
.env.*.local
.env.shared
!.env.example
!.env.shared.example
!.env.test.example

# K8s/app secrets files (ignore real secrets, keep templates/examples)
secrets*.yml
secrets*.yaml
!secrets*.example.yml
!secrets*.example.yaml
!secrets*.template.yml
!secrets*.template.yaml

# Sentry CLI config can contain auth tokens
.sentryclirc

########################
# Local DB artifacts (SQLAlchemy/Alembic dev)
# NOTE: Do NOT ignore migrations/ or alembic/versions/ (those should be committed).
########################
*.db
*.sqlite
*.sqlite3
*.db-journal
*.sqlite-journal
*.sqlite3-journal
*.wal
*.shm
*.dump
*.pgdump

########################
# Docker / local ops
########################
docker-compose.override.yml
docker-compose.*.override.y*ml
docker-compose.*.local.y*ml
*.tar
*.tar.gz
*.tgz

# Optional: local bind-mount convention
.volumes/

########################
# IaC state (safe to include)
########################
.terraform/
*.tfstate
*.tfstate.*

########################
# Logs / temp
########################
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.pid
*.pid.lock
*.seed
*.tmp
*.temp
*.swp
*.swo
*.bak
*.zip

########################
# IDE / OS junk
########################
.vscode/
.idea/
*.iml
*.code-workspace
.DS_Store
Thumbs.db

########################
# Keys / certs (avoid accidental commits)
########################
*.pem
*.key
*.crt
*.pfx
*.p12

########################
# Optional: Prisma generated outputs (harmless if repo doesn't use Prisma)
########################
.prisma/
prisma/generated/
src/prisma/
