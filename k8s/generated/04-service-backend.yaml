apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: synapse            # e.g., flashmaster
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP


