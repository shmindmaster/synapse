apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: synapse            # e.g., flashmaster
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP


